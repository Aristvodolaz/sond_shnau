# Сайт Фонда помощи шнауцерам

Современный веб-сайт фонда, созданный на Nuxt 3, TypeScript и TailwindCSS.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Сайт будет доступен по адресу: `http://localhost:3000`

### Сборка для production

```bash
npm run build
```

### Предпросмотр production-сборки

```bash
npm run preview
```

## 📁 Структура проекта

```
fond/
├── assets/
│   └── css/
│       └── main.css              # Основные стили и Tailwind
├── components/
│   ├── AppHeader.vue             # Хедер с навигацией
│   ├── AppFooter.vue             # Футер
│   ├── DogCard.vue               # Карточка собаки
│   ├── NewsCard.vue              # Карточка новости
│   ├── RssWidget.vue             # RSS виджет
│   ├── UiButton.vue              # Кнопка
│   ├── UiCard.vue                # Карточка
│   ├── UiTag.vue                 # Тег/бейдж
│   └── UiAccordion.vue           # Аккордеон
├── composables/
│   └── useMenuItems.ts           # Пункты меню
├── data/
│   ├── dogs.json                 # Данные о собаках
│   ├── news.json                 # Новости фонда
│   ├── stories.json              # Счастливые истории
│   ├── memorial.json             # В памяти
│   └── adopted.json              # Пристроенные собаки
├── pages/
│   ├── index.vue                 # Главная страница
│   ├── lost-found.vue            # Пропал/найден
│   ├── contacts.vue              # Контакты
│   ├── about/
│   │   ├── index.vue             # Кто мы
│   │   └── rules.vue             # Правила фонда
│   ├── dogs/
│   │   └── [slug].vue            # Детальная страница собаки
│   ├── news/
│   │   └── [slug].vue            # Детальная страница новости
│   ├── results/
│   │   ├── adopted.vue           # Шнауцеры дома
│   │   ├── stories.vue           # Счастливые истории
│   │   └── memorial.vue          # В памяти
│   ├── stories/
│   │   └── [slug].vue            # Детальная страница истории
│   └── useful/
│       ├── faq.vue               # ЧаВо
│       ├── legal.vue             # Юр отдел
│       ├── give-away.vue         # Отдать шнауцера
│       └── adopt.vue             # Взять шнауцера
├── server/
│   ├── api/
│   │   └── rss.get.ts            # API для получения RSS
│   └── routes/
│       └── rss.xml.get.ts        # Генерация RSS-ленты
├── types/
│   └── index.ts                  # TypeScript типы
├── utils/
│   └── config.ts                 # Конфигурация (ссылки, контакты)
├── app.vue                       # Корневой компонент
├── nuxt.config.ts                # Конфигурация Nuxt
├── tailwind.config.ts            # Конфигурация Tailwind
└── package.json
```

## ⚙️ Конфигурация

### Основные настройки

Все внешние ссылки, контакты и конфигурационные параметры находятся в файле:

```
utils/config.ts
```

**Что нужно заменить:**

- `VK_URL` — ссылка на группу ВКонтакте
- `TG_URL` — ссылка на Telegram-канал
- `FORUM_URL` — ссылка на форум
- `LOST_FOUND_FORUM_URL` — ссылка на раздел форума "Пропал/найден"
- `RSS_URL` — URL внешнего RSS (если используется)
- `PHONE` — телефон фонда
- `EMAIL` — email фонда
- `BANK_ACCOUNT`, `BANK_NAME`, `RECIPIENT` — реквизиты для пожертвований

### RSS Feed

RSS-лента генерируется из `data/news.json` и доступна по адресу:

```
/rss.xml
```

Также на главной странице работает RSS-виджет, который подтягивает данные через API (`/api/rss`). По умолчанию он пытается загрузить внешний RSS с `https://pet-help.ru/rss.xml`. Если нужно использовать только внутренний RSS:

1. В `server/api/rss.get.ts` измените `RSS_URL` на `/rss.xml` (внутренний endpoint)
2. Или создайте свою логику получения RSS

### Данные

Все данные хранятся в JSON-файлах в папке `data/`:

- **dogs.json** — собаки на пристройство
- **news.json** — новости фонда
- **stories.json** — счастливые истории
- **memorial.json** — памяти ушедших собак
- **adopted.json** — пристроенные собаки

Для добавления новых собак, новостей или историй — просто редактируйте соответствующие файлы.

### Изображения

Все пути к изображениям в JSON указаны как `/images/...`. 

**Где разместить изображения:**

1. Создайте папку `public/images/` в корне проекта
2. Разместите там изображения согласно структуре:
   ```
   public/
   └── images/
       ├── dogs/
       ├── news/
       ├── stories/
       ├── memorial/
       └── adopted/
   ```

Компонент `<NuxtImg>` автоматически оптимизирует изображения.

## 🎨 Дизайн

- **Цветовая палитра**: тёплая бежево-молочная гамма с оранжевым акцентом (настраивается в `tailwind.config.ts`)
- **Шрифты**: Inter (основной), Manrope (заголовки) — загружаются из Google Fonts
- **Адаптивность**: Mobile-first подход, все компоненты адаптивны

## 📦 Используемые технологии

- **Nuxt 3** — Vue.js фреймворк
- **TypeScript** — типизация
- **TailwindCSS** — стили
- **@nuxt/image** — оптимизация изображений
- **rss-parser** — парсинг RSS

## 🔧 Разработка

### Добавление новой собаки

1. Откройте `data/dogs.json`
2. Добавьте новый объект с полями:
   - `id`, `slug`, `name`, `type`, `age`, `city`
   - `curator` (имя, телефон, email)
   - `photos` (массив путей к фото)
   - `description`, `features`, `health`, `character`
   - `forumTopicUrl`, `status`, `dateAdded`
3. Разместите фото в `public/images/dogs/`

### Добавление новости

1. Откройте `data/news.json`
2. Добавьте объект с полями:
   - `id`, `slug`, `title`, `date`, `preview`, `content`
   - `image` (опционально)
3. Разместите изображение в `public/images/news/`

### Изменение меню

Редактируйте файл `composables/useMenuItems.ts`

## 📝 Лицензия

Проект создан для фонда помощи шнауцерам.

## 💬 Поддержка

Для вопросов и помощи свяжитесь с фондом через контакты на сайте.
# sond_shnau
