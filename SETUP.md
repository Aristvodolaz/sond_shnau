# 🚀 Инструкция по установке и настройке

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Настройка конфигурации

### 2.1 Контакты и ссылки

Откройте файл `utils/config.ts` и замените плейсхолдеры на реальные данные:

```typescript
export const config = {
  // Социальные сети
  VK_URL: 'https://vk.com/ваша_группа',
  TG_URL: 'https://t.me/ваш_канал',
  
  // Форум
  FORUM_URL: 'https://pet-help.ru/forum/',
  LOST_FOUND_FORUM_URL: 'https://pet-help.ru/forum/viewforum.php?f=51',
  
  // RSS
  RSS_URL: 'https://pet-help.ru/rss.xml',
  
  // Контакты
  PHONE: '+7 (XXX) XXX-XX-XX',
  EMAIL: 'info@ваш-email.ru',
  
  // Реквизиты
  BANK_ACCOUNT: 'XXXX XXXX XXXX XXXX',
  BANK_NAME: 'Название банка',
  RECIPIENT: 'ФИО получателя'
}
```

### 2.2 RSS Feed

В файле `server/api/rss.get.ts` замените URL RSS-ленты на актуальный:

```typescript
const RSS_URL = 'https://ваш-домен.ru/rss.xml'
```

В файле `server/routes/rss.xml.get.ts` замените домен сайта:

```typescript
const siteUrl = 'https://ваш-домен.ru'
```

## Шаг 3: Добавление данных

### 3.1 Собаки

Откройте `data/dogs.json` и добавьте/измените данные о собаках. Структура объекта:

```json
{
  "id": "1",
  "slug": "reks",
  "name": "Рекс",
  "type": "riesenschnauzer",
  "age": "5 лет",
  "city": "Москва",
  "curator": {
    "name": "Имя куратора",
    "phone": "+7 (XXX) XXX-XX-XX",
    "email": "email@example.com"
  },
  "photos": ["/images/dogs/reks-1.jpg"],
  "description": "Описание собаки",
  "features": ["Особенность 1", "Особенность 2"],
  "health": "Информация о здоровье",
  "character": "Характер",
  "forumTopicUrl": "https://forum.ru/topic",
  "status": "available",
  "dateAdded": "2024-01-15"
}
```

**Типы шнауцеров:**
- `riesenschnauzer` — Ризеншнауцер
- `mittelschnauzer` — Миттельшнауцер
- `zwergschnauzer` — Цвергшнауцер
- `metis` — Метис

**Статусы:**
- `available` — Ищет дом
- `pensioner` — Пенсионер

### 3.2 Новости

Откройте `data/news.json`:

```json
{
  "id": "1",
  "slug": "news-slug",
  "title": "Заголовок новости",
  "date": "2024-04-10",
  "preview": "Краткое описание",
  "content": "Полный текст новости",
  "image": "/images/news/photo.jpg"
}
```

### 3.3 Истории

Откройте `data/stories.json`:

```json
{
  "id": "1",
  "slug": "story-slug",
  "dogName": "Имя собаки",
  "year": 2023,
  "part": 1,
  "preview": "Краткое описание истории",
  "content": "Полный текст истории",
  "photos": ["/images/stories/photo1.jpg"],
  "beforePhoto": "/images/stories/before.jpg",
  "afterPhoto": "/images/stories/after.jpg"
}
```

### 3.4 В памяти

Откройте `data/memorial.json`:

```json
{
  "id": "1",
  "slug": "dog-slug",
  "name": "Имя",
  "years": "2010 — 2023",
  "type": "Ризеншнауцер",
  "photo": "/images/memorial/photo.jpg",
  "tribute": "Памятный текст",
  "dateAdded": "2023-12-15"
}
```

### 3.5 Пристроенные собаки

Откройте `data/adopted.json`:

```json
{
  "id": "1",
  "name": "Имя",
  "type": "Ризеншнауцер",
  "year": 2023,
  "city": "Город",
  "photo": "/images/adopted/photo.jpg",
  "adoptionDate": "2023-05-12"
}
```

## Шаг 4: Добавление изображений

Разместите изображения в соответствующих папках:

```
public/
└── images/
    ├── dogs/         # Фото собак
    ├── news/         # Фото для новостей
    ├── stories/      # Фото для историй
    ├── memorial/     # Фото для мемориала
    └── adopted/      # Фото пристроенных собак
```

**Рекомендуемые форматы:**
- JPG/JPEG для фотографий
- Размер: 800x800px или больше для собак
- Размер: 1200x600px для новостей

**Важно:** Имена файлов в папках должны совпадать с путями, указанными в JSON-файлах.

## Шаг 5: Favicon

Замените файл `public/favicon.ico` на свою иконку сайта.

## Шаг 6: Запуск

### Development mode (разработка)

```bash
npm run dev
```

Сайт будет доступен по адресу: `http://localhost:3000`

### Production build (сборка для production)

```bash
npm run build
npm run preview
```

## 🎨 Дополнительные настройки

### Изменение цветовой схемы

Откройте `tailwind.config.ts` и измените цвета в секции `theme.extend.colors`:

```typescript
colors: {
  primary: {
    50: '#fef7ee',
    // ... другие оттенки
  }
}
```

### Изменение шрифтов

В файле `assets/css/main.css` замените ссылку на Google Fonts:

```css
@import url('https://fonts.googleapis.com/css2?family=ВашШрифт');
```

И обновите `tailwind.config.ts`:

```typescript
fontFamily: {
  sans: ['ВашШрифт', 'system-ui', 'sans-serif']
}
```

### Изменение меню

Редактируйте файл `composables/useMenuItems.ts`

## 📱 Деплой

### Vercel

```bash
npm install -g vercel
vercel
```

### Netlify

```bash
npm run build
# Загрузите папку .output/public в Netlify
```

### VPS/сервер

```bash
npm run build
# Скопируйте .output на сервер
# Запустите с помощью node .output/server/index.mjs
```

## 🔍 Проверка перед запуском

- [ ] Заменены все контакты в `utils/config.ts`
- [ ] Добавлены реальные данные в JSON файлы
- [ ] Загружены изображения в `public/images/`
- [ ] Проверены все внешние ссылки (форум, соцсети)
- [ ] Заменён favicon
- [ ] RSS настроен корректно
- [ ] Проведено тестирование на разных устройствах

## ❓ Частые вопросы

**Q: Изображения не отображаются**  
A: Убедитесь, что пути в JSON совпадают с реальными путями к файлам в `public/images/`

**Q: RSS не работает**  
A: Проверьте URL в `server/api/rss.get.ts` и убедитесь, что RSS-лента доступна

**Q: Ошибка при сборке**  
A: Запустите `npm install` и убедитесь, что все зависимости установлены

**Q: Как добавить новую страницу?**  
A: Создайте файл .vue в папке `pages/`, он автоматически станет доступен по соответствующему маршруту

## 📞 Поддержка

При возникновении проблем обращайтесь к документации Nuxt 3: https://nuxt.com/docs
